let points = 0
            let pointsTracker = document.getElementsById("pointsTracker")

            function increasePoints() {
                points = points + 1
                pointsTracker.innerText = points + 'Pet Points'
    
            }

function pupgrades () {

function gameLoop(){
    points=points + (0.2 * pets)
    pointsTracker.innerText = points.toFixed(2) + 'Pet Points'
}
}